{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "permissions": {
    "allow": [
      "Bash(python3 -m quarkcosmos_levels *)",
      "Bash(python3 -m pytest*)",
      "Bash(pip install -e*)",
      "Bash(ruff check*)",
      "Bash(./gradlew *)"
    ]
  },
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "[ \"$CLAUDE_CODE_REMOTE\" = \"true\" ] || exit 0; cd \"$CLAUDE_PROJECT_DIR/levels-builder\" 2>/dev/null || exit 0; python3 -c 'import quarkcosmos_levels, pytest, ruff' >/dev/null 2>&1 || python3 -m pip install -q -e '.[dev]' >/dev/null 2>&1 || true",
            "timeout": 120
          }
        ]
      }
    ]
  }
}
